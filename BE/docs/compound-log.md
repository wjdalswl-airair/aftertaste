# Compound Log

같은 실수가 반복되지 않도록, compound 에이전트가 각 단계 실행 결과에서 찾은 문제와 반복 패턴을 누적 기록하는 문서.

새 기록은 맨 위(최신순)에 추가한다. 기존 기록은 지우거나 고치지 않는다.

## 기록 형식

```
## {YYYY-MM-DD}
- tags: [{키워드1}, {키워드2}, ...]
- category: {구현 실수 | 테스트 놓침 | 문서·코드 불일치 | 리뷰 누락 | 하네스 개선 | ...}
- 문제: {무엇이 어떻게 잘못됐는지, 몇 번째 반복인지}
- 개선: {어떤 하네스를 어떻게 고쳤는지, 혹은 왜 아직 고치지 않았는지}
```

---

## 2026-08-20 (Phase 4 코스 사이클)
- tags: [phase4, courses, CourseDetailView, perform_authentication, 인증, 반복3번째, coding.md]
- category: 구현 실수 (반복, 3번째)
- 문제: `CourseDetailView`(GET은 비로그인 허용, PATCH/DELETE는 로그인 필요)에서 `perform_authentication` 오버라이드가 메서드 구분 없이 뷰 전체에 걸려 있어, 무효 토큰으로 PATCH/DELETE를 시도해도 로그인 요건 자체는 안 깨지지만 에러 메시지가 다른 앱과 달라진다("다시 로그인하세요" 대신 "로그인이 필요한 기능입니다"). 2026-08-19 기록에서 reviews의 같은 문제를 발견했을 때 "로그인 필요/불필요 메서드가 한 뷰에 섞여 있을 때"라는 구체적 조건에서 재발하는지 지켜보기로 했는데, 이번에 정확히 그 조건에서 재현됐다(review 표현: "reviews에 이어 2번째/3번째 사례"). 지켜보기로 한 조건이 실제로 재발을 예측한 셈이라 규칙을 추가할 시점으로 판단했다.
- 개선: `.claude/agents/coding.md`의 기존 `perform_authentication` 문단에 "한 뷰 안에 로그인 필요 메서드와 불필요 메서드가 섞여 있으면, 오버라이드를 뷰 전체가 아니라 메서드 단위로 걸어서 로그인 필요 메서드의 에러 메시지 관례가 달라지지 않게 한다"는 문장을 추가했다. 이번 사이클에서는 BLOCKING이 아닌 NIT이라 courses 앱 코드 자체는 고치지 않았다 — 다음에 이 코드를 다시 만지거나 비슷한 뷰를 새로 만들 때 이 규칙을 적용한다.

## 2026-08-20 (Phase 4 코스 사이클)
- tags: [phase4, courses, 브랜치병합, merge, conflict, 전체테스트, 정상동작, CLAUDE.md]
- category: 하네스 개선 (예방적)
- 문제: coordinator가 `feature/be/personalization`, `feature/be/multi-language`를 `feature/be/course`로 병합하다가 5개 파일에서 충돌이 났다. 전부 "양쪽이 같은 자리 근처에 서로 다른 내용을 추가"한 형태로 실제 로직 충돌은 아니었지만, coordinator가 한쪽만 택하지 않고 두 기능을 모두 살리는 방향으로 직접 해결했고, 병합 직후 전체 테스트(280개)를 돌려 통과를 확인한 뒤 커밋했다. 이 프로젝트가 여러 기능 브랜치를 병행 개발하고 나중에 합치는 구조라는 점은 2026-08-20 앞선 기록("Phase 문서가 가정하는 이전 Phase 코드가 지금 브랜치엔 없음")에서 이미 "구조적으로 반복될 가능성이 크다"고 판단했었는데, 이번엔 그 예측대로 실제 병합·충돌 해결까지 처음으로 실행됐다. 처리 결과 자체는 문제없었다(정상 동작).
- 개선: 결과는 정상이었지만 "병합 직후 전체 테스트를 돌려 두 기능이 서로 깨지지 않았는지 확인한다"는 절차가 문서에 명문화돼 있지 않아 이번엔 우연히 지켜진 것에 가까웠다. 이 구조가 앞으로도 반복될 프로젝트라 예방적으로 `CLAUDE.md`에 "여러 기능 브랜치를 병합해 충돌을 해결할 때는 어느 한쪽만 취하지 말고 두 기능을 모두 보존하는 방향으로 병합하고, 반드시 전체 테스트를 실행해 통과를 확인한 뒤에 커밋한다"는 절차를 추가했다.

## 2026-08-20 (Phase 4 코스 사이클)
- tags: [phase4, courses, 질문우선, 자기신고, 질문거부, 위임, 첫사례, coding.md]
- category: 리뷰 누락 (정상 동작 사례, 새로운 상황)
- 문제: `docs/PHASES/PHASE4.md`에 미결로 남은 코스 설계(데이터 모델, 주변 상권 저장·폐업 처리, 생성 주체)를 coordinator가 AskUserQuestion으로 물었으나, 사용자가 그 질문 자체를 거부하고 "진행해줘"라고만 답했다. 기존 원칙("불명확하면 멈추고 물어본다")과 배치되는 것처럼 보이지만, 사용자가 질문 경로를 원치 않는다는 신호를 두 번(질문 거부 + "진행해줘") 줬다는 점이 이전 "질문 우선" 사례들과 다르다. coordinator는 질문을 반복하지 않고 스스로 결정한 뒤 `docs/DETAIL_SPEC.md` 6-1 #17에 "AI가 임의로 정함"으로 명시적으로 표시하고 나서 구현을 진행했다. 이 대응은 "질문 우선" 원칙을 어긴 게 아니라, 사용자가 질문 경로를 명시적으로 닫았을 때 예비 경로인 "자기신고"로 전환한 것으로 판단했다 — 기존에 여러 차례 확인된 자기신고 패턴이 이 새로운 상황(질문 자체를 거부당함)에서도 잘 작동한 사례다. 이 구체적 상황(사용자가 질문 자체를 명시적으로 거부)은 이번이 첫 사례다.
- 개선: 대응 자체는 적절했다고 판단해 기존 "질문 우선"·"자기신고" 규칙을 뒤집는 방향으로는 고치지 않았다. 다만 이 상황에서 무엇을 해야 하는지가 문서에 없어 coordinator가 즉석에서 판단해야 했으므로, `.claude/agents/coding.md`의 "요구사항에 없는 내용을 임의로 채우지 않는다" 문단 뒤에 "사용자가 질문을 거부하고 '알아서 진행해달라'고 위임하면, 질문을 반복하지 말고 대신 결정한 내용과 이유를 기준 문서와 완료 보고에 명시적으로 표시(자기신고)한 뒤 진행한다"는 문장을 추가해, 다음에 같은 상황이 나와도 즉석 판단 없이 바로 따를 수 있게 했다.

## 2026-08-20 (Phase 4 코스 사이클)
- tags: [phase4, courses, favorites, reviewNIT, 트랜잭션, 레이스컨디션, N+1, 좌표검증, 첫사례]
- category: 구현 실수 (경미, NIT)
- 문제: review가 courses/favorites 구현에서 BLOCKING 없이 NIT 4건을 추가로 찾았다: (1) `CourseWriteSerializer.update()`에서 title/description 저장과 course_places 삭제+재생성이 서로 다른 트랜잭션이라, 후자에서 예외가 나면 코스가 부분 반영 상태로 남을 수 있음 (2) 동시 PATCH 요청 시 `unique_together(course, role)` 위반으로 500이 날 수 있는 레이스 컨디션 (3) `MyCourseListView`/`PlaceCourseListCreateView`/`MyFavoriteListView`가 `course_places`는 prefetch하지만 `place`/`creator` FK는 select_related를 안 해 N+1 발생 가능(과거 위치 기반 기능 성능 이슈와 같은 결) (4) `CoursePlace` 위경도에 범위 검증(-90~90, -180~180)이 없음. 넷 다 이번이 첫 발견이고 BLOCKING이 아니라 이번 사이클에서는 고치지 않았다. 참고로 review는 CharField 길이 검증(과거 반복 지적됐던 문제)이 `ModelSerializer` 자동 검증 덕에 이번엔 재현되지 않았다는 점도 확인했다.
- 개선: 넷 다 첫 사례라 하네스 규칙은 아직 추가하지 않음. (1)은 2026-08-20 앞선 기록("재시도·재작업 실패 시 정상이던 상태값을 덩달아 리셋")과는 다르지만 "여러 저장을 한 트랜잭션으로 묶지 않아 부분 반영 상태가 남는" 유형으로, 다음에 같은 유형이 한 번 더 나오면 규칙 추가를 검토한다. (3) select_related 누락은 이 프로젝트에서 이미 여러 차례(주변 상권, 추천 등) 성능 NIT으로 지적된 결이라, 재발 시 `.claude/agents/coding.md`에 "목록 API를 만들 때는 prefetch_related뿐 아니라 FK도 select_related로 함께 확인한다"는 규칙 추가를 검토한다. (2)·(4)는 이번이 첫 발견이라 계속 지켜본다.

## 2026-08-20
- tags: [phase4, places, translation, is_approved, 재번역, review, BLOCKING]
- category: 구현 실수
- 문제: `places/translation.py`의 `_apply_translation`이, 재번역이 **완전히 실패**해서 내용이 하나도 안 바뀌었을 때도 `is_approved`를 무조건 `False`로 리셋하고 있었다. 그러면 이미 관리자가 승인해서 손님에게 정상적으로 보이던 번역이, 관리자가 "다시 번역"을 눌렀는데 API가 일시적으로 실패한 것 한 번만으로 화면에서 사라져버린다. review가 정적 분석으로 [BLOCKING]으로 찾아냈고, coding이 "실제로 값이 갱신된 필드가 있을 때만 is_approved를 리셋"하도록 고치고 회귀 테스트(`test_complete_failure_keeps_existing_approval`)를 추가해 187개 테스트 통과를 확인했다. "재시도/재작업이 실패했을 때 이미 정상이던 상태값까지 덩달아 리셋해버리는" 이 구체적 유형은 이번이 첫 발견이라 아직 반복 패턴으로 보기는 이르다. review는 이 외에도 NIT 3건(200자 트렁케이션이 SUCCESS로 표시돼 관리자가 잘림을 못 알아챔 / 대량 import 시 `transaction.on_commit` 동기 호출로 인한 지연·예외 전파 가능성 / `lang`·`Member.language` 값 정규화 부재)을 남겼는데 전부 이번이 첫 발견이라 이번 사이클에서는 고치지 않았다.
- 개선: BLOCKING은 이번 사이클에서 바로 수정해 반영했다. 첫 발견이라 하네스 규칙은 아직 추가하지 않는다. 다음에 "재시도·재작업 실패 시 정상이던 상태값을 덩달아 리셋하는" 유형이 한 번 더 나오면 `.claude/agents/coding.md`에 관련 규칙 추가를 검토한다. 남겨둔 NIT 3건도 각각 다시 나오면(트렁케이션 표시 부정확, 대량 처리 동기 호출, 외부 입력 값 정규화 누락) 관련 규칙 추가를 검토한다.

## 2026-08-20
- tags: [phase4, places, PlaceTranslation, WorkTranslation, CharField, max_length, 외부API번역, coding.md, test.md]
- category: 구현 실수 (반복, 2번째)
- 문제: `PlaceTranslation.name`/`WorkTranslation.title`이 `CharField(max_length=200)`인데, Google Translate가 돌려준 번역문이 이 길이를 넘으면 저장 시 `DataError`가 나는 버그를 test 단계에서 DETAIL_SPEC 4-3의 예외 케이스(길이비율 경계값 등)를 직접 재검증하다가 찾아냈다. 2026-08-18에 기록한 `SearchHistory.keyword` 초과 버그와 근본 원인 유형은 같다(모델 `max_length`와 저장 전 값 길이가 항상 일치하지 않음)지만, 이번엔 사용자가 직접 입력한 값이 아니라 **외부 API(번역 서비스) 응답값**이 원인이었다. 당시 추가한 `.claude/agents/coding.md` 규칙 문구가 "사용자 입력을 CharField/TextField에 저장할 때는..."으로 한정돼 있어서, 외부 API 응답값을 저장하는 이번 케이스는 문구상 명시적으로 커버되지 않았고 실제로 coding 단계에서 놓쳤다(test 단계에서 뒤늦게 발견). "저장 전 검증 없이 CharField/TextField에 값을 쓰는" 문제가 원인 소스를 바꿔가며 2번째로 재현된 것으로, 이 프로젝트가 외부 API(공공데이터, 번역 등)를 계속 연동할 예정이라 앞으로도 반복될 가능성이 있다.
- 개선: 사용자에게 확인(AskUserQuestion)해서 기존 SearchHistory 사례와 동일하게 "200자로 잘라서 저장"으로 처리하고 회귀 테스트(`test_translated_name_over_200_chars_is_truncated_and_saved`)를 추가했다. 재발 방지를 위해 `.claude/agents/coding.md`와 `.claude/agents/test.md`의 기존 CharField/TextField 규칙 문구를 "사용자 입력"에서 "사용자 입력이든 외부 API 응답값이든"으로 일반화해서, 같은 유형이 세 번째 다른 소스(예: 다른 외부 API)에서 나와도 문구상 이미 커버되도록 고쳤다.

## 2026-08-20
- tags: [phase4, multi-language, 브랜치범위, place, work, coding전확인, 질문우선, coding.md]
- category: 문서·코드 불일치
- 문제: `docs/PHASES/PHASE4.md`가 "이전 Phase에서 만든 것을 이어받는다"고 전제하는데, 실제 작업 브랜치(`feature/be/multi-language`)는 Phase 3(`feature/be/personalization`)이 아니라 Phase 2(`feature/be/place-description`) 기준으로 만들어져 있어서 `reviews` 앱 자체가 브랜치에 없었다. 그래서 Phase 4 다국어 번역(N-01) 중 리뷰 번역을 지금 브랜치에서 그대로 구현하려 했다면 존재하지 않는 모델을 참조하는 코드를 만들 뻔했다. coding이 착수 전 "기준 문서 확인" 단계에서 문서와 실제 코드 상태를 대조해보다가 발견했고, 임의로 건너뛰거나 흉내 내지 않고 사용자에게 확인해서 "명소·작품 번역만 먼저, 리뷰 번역은 브랜치가 합쳐진 뒤 별도 진행"으로 범위를 좁혔다. 이 구체적 유형("Phase 문서가 가정하는 이전 Phase 코드가 지금 브랜치엔 없음")은 이번이 첫 발견이지만, 이 프로젝트는 여러 기능 브랜치(`place-description`/`personalization`/`multi-language`/`login` 등)를 병행해서 만들고 나중에 합치는 구조라, 브랜치 병합 순서에 따라 앞으로도 같은 유형이 반복될 가능성이 구조적으로 크다고 판단했다(과거 "구조적으로 반복될 카테고리는 1회만 나와도 하네스에 반영" 기준과 동일).
- 개선: 반복 가능성이 구조적이라 첫 발견 시점에 바로 규칙을 추가한다. `.claude/agents/coding.md`의 "3. 구현하기" 절에 "Phase 문서가 이전 Phase 코드를 이어받는다고 전제하는 기능을 만들기 전에는 실제 작업 브랜치에 그 앱·모델이 있는지 직접 확인하고, 없으면 범위를 어떻게 좁힐지 사용자에게 먼저 확인한다"는 규칙을 추가했다. 또한 이 사이클에서 번역 방식·지원 언어(문서 미결 항목)와 응답 언어 결정 순서(문서에 없던 부분)를 코드 작성 전에 AskUserQuestion으로 먼저 확인한 것도 기존 "질문 우선" 패턴이 다시 잘 작동한 사례라 별도 규칙 추가는 하지 않았다.

## 2026-08-19 (Phase 3 사이클 B)
- tags: [phase3, recommendation, _nearest_places, 성능, 재확인, coding.md]
- category: 하네스 개선
- 문제: 2026-08-19(사이클 A)에 review가 `_nearest_places`(좌표 있는 명소 전체를 매번 파이썬에서 정렬)의 성능 NIT을 처음 지적했을 때, "다음 위치 기반 기능을 만들 때 다시 나오면 검토한다"는 기준으로 규칙 추가를 보류했었다. 이번 사이클 B에서 로그인 사용자 개인화 추천(거리+검색이력+즐겨찾기+리뷰 가산점)의 후보 풀 계산에 같은 `_nearest_places`가 재사용됐고, review가 같은 유형의 NIT을 다시 지적했다. 새 문제가 아니라 기존 NIT의 재확인이지만, 앞서 정해둔 "다음 위치 기반 기능에서 다시 나오면 검토한다"는 조건을 정확히 충족했다.
- 개선: `.claude/agents/coding.md`의 "3. 구현하기" 절에 "위치·거리 기반 기능을 만들거나 기존 위치 계산 함수(`_nearest_places` 등)를 재사용할 때는 전체 스캔 정렬의 성능 영향을 인지하고, 같은 함수가 두 번째로 재사용되는 시점에는 DB 레벨 정렬/인덱스 개선이 필요한지 확인한다"는 규칙을 추가했다. 실제 정렬 방식(DB 레벨 정렬로 바꾸는 등)을 언제 고칠지는 제품 코드를 다루는 coding/PHASE 문서에서 사용자와 별도로 정할 문제이므로, 이 기록에서는 하네스(인지·재확인 절차) 개선까지만 다룬다.

## 2026-08-19 (Phase 3 사이클 B)
- tags: [phase3, timezone, UTC, KST, 명예의전당, 첫사례, coding.md]
- category: 구현 실수 (경미, NIT)
- 문제: review가 명예의 전당의 "이번 달" 계산이 서버 `TIME_ZONE=UTC` 기준이라 한국 사용자가 체감하는 "이번 달"(KST)과 어긋날 수 있다는 NIT을 지적했다(예: KST 기준 매월 1일 오전 9시 이전 작성 리뷰가 UTC 기준으로는 지난달로 집계됨). `USE_TZ` 설정과는 일관돼 있어 버그는 아니고, 문서에도 시간대 기준이 명시돼 있지 않아 위반도 아니다. 이번이 첫 발견이지만, 이 프로젝트는 한국 이용자 대상 서비스이고 Phase 4 이후에도 "오늘"·"최근 N일" 같은 날짜 기준 통계·이벤트 기능을 만들 가능성이 높아, 서버 타임존과 사용자 체감 시간대 불일치는 특정 기능 하나의 우연한 결함이 아니라 날짜 계산이 들어가는 기능마다 구조적으로 반복될 수 있는 카테고리로 판단했다(2026-08-13 "경기 데이터 드림 출처 검증", 2026-08-18 "CharField max_length" 사례처럼 구조적으로 반복될 카테고리는 1회만 나와도 하네스에 반영한다는 기존 기준과 동일하게 적용).
- 개선: 첫 사례이지만 구조적 반복 가능성이 높다고 판단해 바로 규칙을 추가했다. `.claude/agents/coding.md`의 "3. 구현하기" 절에 "'이번 달'·'오늘'·'최근 N일' 등 날짜·시간 기준 집계·필터링 기능을 만들 때는 서버 `TIME_ZONE` 설정이 서비스 대상 시간대(KST)와 일치하는지 먼저 확인하고, 문서에 시간대 기준이 없으면 사용자에게 확인한다"는 규칙을 추가했다. 실제 `TIME_ZONE`을 KST로 바꿀지, 계산 로직만 KST로 변환할지는 제품 코드 결정이라 이 하네스 기록에서는 다루지 않는다.

## 2026-08-19 (Phase 3 사이클 B)
- tags: [phase3, 질문우선, 자기신고, DETAIL_SPEC우선, 정상동작, 재확인]
- category: 리뷰 누락 (정상 동작 사례)
- 문제 아님 — coding이 착수 전 로그인 사용자 개인화 추천 알고리즘이 기준 문서에 없어 사용자에게 먼저 물어 확정한 뒤 구현한 것, 구체적 가중치·명예의 전당/Top10 API의 URL·응답 스키마를 스스로 정하면서 완료 보고에 자기신고한 것, API 명세서(xlsx)와 DETAIL_SPEC/PHASE3.md가 충돌할 때 후자를 우선하며 이 판단도 자기신고한 것 모두 기존에 여러 차례(2026-08-13, 08-17, 08-18, 08-19 등) 확립된 "질문 우선"·"자기신고" 패턴이 그대로 재현된 사례다. "DETAIL_SPEC/PHASE 우선" 판단도 사이클 A와 동일한 패턴이 반복 확인됐다.
- 개선: 규칙이 의도대로 계속 잘 작동하고 있으므로 하네스를 고치지 않음. `karpathy-guidlines.md`의 질문 우선 규칙과 `.claude/agents/coding.md`의 "요구사항에 없는 내용을 임의로 채우지 않는다"·완료 보고 규칙을 그대로 유지.

## 2026-08-19
- tags: [phase3, favorites, reviews, PlaceReviewListCreateView, 인증, 비로그인API, 반복, coding.md]
- category: 구현 실수
- 문제: `docs/DETAIL_SPEC.md`대로 로그인 없이도 호출 가능해야 하는 `PlaceReviewListCreateView`(리뷰 목록 조회)가 무효·만료 토큰을 보내면 401로 막혀버리는 버그가 coding 구현 시점에 또 들어갔다. 2026-08-18에 `LocaleView`에서 똑같은 유형(비로그인 허용 API에서 `perform_authentication()` 오버라이드 누락)이 발견돼 `.claude/agents/coding.md`에 이미 일반 규칙을 추가했었는데, 이번 사이클에서 그 규칙이 있었음에도 새 뷰를 만들 때 다시 놓쳤다 — 이 구체적 패턴이 확인된 것은 이번이 2번째. 다행히 test 단계에서 test.md에 있는 "무효·만료 토큰" 체크리스트 항목대로 실제로 검증해 즉시 잡아냈고, coding이 `LocaleView` 등 기존 해결 패턴을 그대로 따라 즉시 수정해 재검증까지 통과했다. 규칙은 있었지만 "원칙을 기억해서 매번 적용"하는 방식이라, 새 뷰를 만들 때 기존에 이미 적용된 뷰 목록과 대조하는 절차가 없어 재발한 것으로 보인다.
- 개선: `.claude/agents/coding.md`의 "3. 구현하기" 절 기존 규칙(로그인 무관 API의 무효 토큰 처리) 뒤에 "로그인 여부와 상관없이 호출 가능한 API를 새로 만들 때는 원칙만 기억해서 적용하지 말고, 이미 이 패턴을 적용한 기존 뷰(예: `LocaleView`)를 먼저 찾아 어떻게 처리했는지 확인한 뒤 새 뷰에도 빠짐없이 같은 처리를 적용했는지 대조한다"는 문장을 추가해 절차를 구체화했다. test.md의 관련 체크리스트 규칙은 이번에도 정상 작동했으므로 그대로 유지.

## 2026-08-19
- tags: [phase3, favorites, reviews, reviewNIT, TextField, 응답관례, 첫사례]
- category: 구현 실수 (경미, NIT)
- 문제: review가 favorites/reviews 구현에서 BLOCKING 없이 NIT 3건을 찾았다: (1) `PlaceReviewListCreateView`에서 `perform_authentication` 오버라이드가 GET/POST 모두에 적용돼, 로그인이 필요한 POST를 무효 토큰으로 시도하면 다른 앱(즐겨찾기/좋아요)과 다른 에러 메시지가 나옴 — 로그인 요건 자체는 안 깨지지만, 로그인 필요/불필요 메서드가 한 뷰에 섞여 있을 때 오버라이드를 메서드 구분 없이 적용하면 생기는 새로운 유형의 부작용 (2) `Review.content`의 `TextField(max_length=1000)`이 DRF 시리얼라이저 레벨에서만 강제되고 DB/모델 레벨에는 없어, admin이나 시리얼라이저를 안 거치는 경로에서는 1000자 제한이 안 걸림 (3) `ReviewReportView.post`가 중복 신고여도 항상 201을 반환해 즐겨찾기/좋아요의 200/201 구분 관례와 다름. 셋 다 이번이 첫 발견이고 당장 오동작을 일으키는 BLOCKING은 아니라 이번 사이클에서는 고치지 않고 넘어갔다.
- 개선: 셋 다 첫 사례라 하네스 규칙은 아직 추가하지 않음. (1)은 "로그인 필요 메서드와 불필요 메서드가 한 뷰에 섞인 경우"라는 좀 더 구체적인 조건에서 재발하는지 지켜본다. (2)는 2026-08-18 "CharField/TextField max_length와 저장 전 검증 불일치"와 방향은 다르지만(그때는 초과 입력이 DB 에러를 냈던 문제, 이번은 시리얼라이저만 강제하고 DB/모델 레벨엔 없는 문제) 같은 카테고리로 볼 수 있어, 다음에 TextField/CharField 길이 제한을 시리얼라이저에만 두는 사례가 한 번 더 review에서 지적되면 `.claude/agents/coding.md`에 "글자수 제한은 시리얼라이저뿐 아니라 모델 `validators`로도 이중으로 건다" 같은 규칙 추가를 검토한다. (3)은 응답 코드 관례 문제로, 앱마다 다른 관례가 한 번 더 지적되면 API 응답 관례를 문서(DETAIL_SPEC 등)에 명문화하는 것을 검토한다.

---

## 2026-08-19
- tags: [phase2-5, places, kakao, 외부API, 순차호출, reviewNIT, 코스추천예고]
- category: 구현 실수 (경미, NIT)
- 문제: review가 "주변 상권" 조회(음식점FD6+카페CE7+관광명소AT4)에서 NIT 3건을 더 찾아냈다: (1) 카카오 카테고리 검색 3번을 순차로(하나씩 기다렸다가 다음 호출) 부르고 각각 10초 타임아웃이라, 최악의 경우 한 번의 명소 상세 조회가 최대 30초까지 걸릴 수 있음 (2) 세 카테고리 결과를 합친 뒤 상한선(15개)으로 자를 때 먼저 채워지는 카테고리(FD6)로 쏠릴 수 있고, 카카오 호출에 `sort=distance` 파라미터를 안 줘서애초에 카테고리 내 정렬도 거리순이 보장되지 않음 (3) 카카오 응답의 `id`가 최종 응답 시리얼라이저에서 빠져 있어 프론트가 리스트를 렌더링할 때 안정적인 key로 못 씀. 셋 다 당장 오동작은 아닌 NIT라 이번 사이클에서는 고치지 않고 넘어갔다. 이번이 첫 발견이지만, 이 프로젝트는 Phase 4에서 "주변 상권 포함 추천 관광코스"처럼 외부 API를 여러 번 순차 호출하고 합쳐서 자르는 기능을 다시 만들 가능성이 높아, (1)·(2) 유형(외부 API 순차 호출 지연, 합치기·자르기 시 정렬 기준 없음)은 반복될 소지가 크다고 판단했다.
- 개선: 첫 사례이고 NIT(당장 오동작 아님)이라 하네스 규칙은 아직 추가하지 않음. Phase 4(추천 코스 등)에서 외부 API를 여러 번 호출해 합치는 기능을 만들 때 review에서 같은 유형(순차 호출로 인한 응답 지연, 정렬 기준 없이 합치고 자르기, 외부 응답의 식별자 누락)이 다시 NIT/BLOCKING으로 나오면, `.claude/agents/coding.md`에 "외부 API를 여러 번 호출해 합칠 때는 응답 시간(순차/병렬)과 정렬 기준, 응답 데이터의 안정적 식별자 보존을 함께 고려한다"는 규칙 추가를 검토한다.

## 2026-08-19
- tags: [phase2-5, places, 가짜테스트, cap, review, test.md]
- category: 테스트 놓침
- 문제: "주변 상권을 15개로 제한(cap)한다"는 로직을 검증하는 테스트가, 카테고리(FD6/CE7/AT4) 3개의 mock 응답에 전부 같은 id를 재사용해서 만들어져 있었다. 그 결과 세 카테고리를 합친 뒤 "중복 제거"만 해도 이미 15개 이하로 줄어들어버려, 정작 확인하려던 "자르기(cap)" 로직 자체는 한 번도 실행되지 않은 채로 테스트가 통과하고 있었다("가짜로 통과하는 테스트"). review가 mock 데이터 구성을 직접 읽고 이 문제를 짚어냈고, 각 카테고리에 서로 다른 20개씩(총 60개 고유 id)을 주도록 test 단계에서 고쳐 실제로 cap 동작을 검증하도록 재작성·재실행해서 통과를 확인했다. 이번이 이 구체적 유형(여러 소스를 합치고 자르는 로직인데, mock 데이터의 id가 겹쳐서 정작 자르기 전에 이미 개수가 줄어드는 바람에 자르기 로직이 실행조차 안 되는 테스트)의 첫 발견이다. 다만 "여러 출처를 합쳐 중복 제거 후 상한선으로 자른다"는 패턴은 Phase 4(코스 추천 등)에서도 나올 법한 구조라 재발 가능성이 있다고 판단했다.
- 개선: 첫 사례지만 구조적으로 반복될 가능성이 있는 카테고리(여러 출처 합치기+중복제거+cap)라 바로 규칙을 추가한다. `.claude/agents/test.md`의 "1. 체크리스트 만들기" 절에 "여러 출처를 합치고 중복 제거·개수 제한(cap) 등으로 줄이는 로직을 테스트할 때는, mock 데이터의 식별자(id 등)가 서로 겹치지 않게 구성해서 중복 제거만으로 이미 목표 개수 이하가 되어버리지 않는지 확인한다. 그래야 뒤쪽 로직(자르기 등)이 실제로 실행되는 조건에서 검증된다"는 규칙을 추가했다.

## 2026-08-19
- tags: [phase2-5, coding, 자기신고, 질문우선, 정상동작]
- category: 리뷰 누락 (정상 동작 사례)
- 문제 아님 — coding 에이전트가 "주변 상권을 어떤 카카오 검색 기준으로 모을지"가 기준 문서에 없어 임의로 "맛집" 키워드 검색을 택해 구현했는데, 완료 보고에서 이 부분을 스스로 "제가 임의로 정한 부분"이라고 명시했다. 그 덕분에 사용자가 바로 확인할 수 있었고, 실제로는 카테고리 검색(FD6+CE7+AT4)이 맞다는 걸 확인해 재작업 지시로 이어졌다. `.claude/agents/coding.md`의 "요구사항에 없는 내용을 임의로 채우지 않는다"는 원칙과 맞물려, 만약 임의로 채운 부분을 못 정했더라도 최소한 "여기는 제가 임의로 정했습니다"라고 밝히는 자기신고가 실제로 재작업을 앞당기는 데 유용했다. 이 자기신고 패턴 자체는 coding.md에 이미 "요구사항에 없는 내용을 임의로 채우지 않는다"는 원칙이 있고 이번에 그 원칙이 보고 단계까지 잘 이어져 작동한 사례이므로, 별도 규칙을 새로 만들 필요는 없다고 판단했다.
- 개선: 규칙이 의도대로 작동했으므로 하네스를 고치지 않음. `.claude/agents/coding.md`의 "요구사항에 없는 내용을 임의로 채우지 않는다" 및 "4. 완료 내용 보고하기"의 항목별 보고 규칙을 그대로 유지. 이 자기신고 패턴이 다음에도 반복 확인되면, coding.md의 "4. 완료 내용 보고하기"에 "임의로 정한 부분은 보고에 명시적으로 표시한다"는 문구를 명문화하는 것을 검토한다.

## 2026-08-19
- tags: [phase2-4, places, RecommendationView, 성능, 입력검증, reviewNIT]
- category: 구현 실수 (경미, NIT)
- 문제: review가 `RecommendationView`/`places/services.py`에서 BLOCKING 없이 NIT 4건을 찾아냈다: (1) `_nearest_places`가 좌표 있는 명소 전체를 매번 파이썬에서 정렬해, 데이터가 늘어나면 느려질 수 있고 `services.py`의 다른 주석이 가정한 규모와 어긋남 (2) `order_by('?')`가 Postgres에서 느린 안티패턴(지금 규모는 문제없음) (3) 공용 함수 `to_decimal`이 `"nan"`/`"inf"` 문자열도 유효 숫자로 통과시켜 극단적 입력에서 정렬 순서가 보장 안 될 수 있음 (4) 좌표 범위 검증 없음(스펙에 없어 위반은 아님). 넷 다 지금 당장 오동작을 일으키지는 않는 NIT이라 이번 사이클에서는 수정하지 않고 넘어갔다. 넷 다 이번이 첫 발견이라 아직 반복 패턴이라 보기는 이르지만, 이 프로젝트는 앞으로도 위치 기반 기능(주변 상권 추천 코스 등)을 계속 만들 예정이라 (1)·(3) 유형(전체 스캔 정렬, 숫자 입력 검증 부족)은 다음 Phase에서 다시 나올 가능성이 있다.
- 개선: 첫 사례이고 NIT(당장 오동작 아님)이라 하네스 규칙은 아직 추가하지 않음. 다음 위치 기반 기능(예: 주변 상권 추천 코스)을 만들 때 review에서 같은 유형(전체 스캔 정렬 성능, 숫자 입력값 nan/inf 미검증)이 다시 NIT/BLOCKING으로 나오면, `.claude/agents/coding.md`에 "위치·거리 계산은 데이터 규모 증가를 고려해 설계한다"거나 숫자 파싱 유틸의 입력 검증 규칙을 추가하는 것을 검토한다.

## 2026-08-19
- tags: [phase2-4, PRD미결항목, 질문우선, 가정검증, RecommendationView]
- category: 리뷰 누락 (정상 동작 사례)
- 문제 아님 — `docs/PRD.md` 6장 "남은 결정 사항 요약"에 미결로 남아있던 "위치 권한 거부 시 추천 기준"(F-04)을 coding 착수 전에 먼저 확인했고, 사용자의 첫 제안("즐겨찾기가 가장 많은 명소 3곳")이 실제로는 Favorite 모델이 코드에 아직 없어 구현 불가능하다는 걸 확인한 뒤, 임의로 비슷한 것으로 대체하지 않고 사용자에게 다시 물어 "무작위 3곳"으로 재확정했다. 이 결정을 `docs/PHASES/PHASE2.md`와 `docs/PRD.md`에 코드 작성 전에 먼저 반영했다. 지금까지 기록된 "질문 우선" 정상동작 사례들(2026-08-17, 2026-08-18 x2)은 모두 "coding이 스스로 정하지 않고 사용자에게 물었다"는 지점까지였는데, 이번엔 "사용자의 답변을 그대로 받아들이지 않고 실제 코드베이스 상태와 맞는지 재확인한 뒤 다시 물었다"는 세부 동작이 새로 관찰됐다.
- 개선: 규칙이 잘 작동했고 아직 1회성 세부 사례라 하네스 규칙을 새로 추가하지는 않음. `karpathy-guidlines.md`의 질문 우선 규칙과 `.claude/agents/coding.md`의 "요구사항에 없는 내용을 임의로 채우지 않는다" 규칙을 그대로 유지. 다음에도 "사용자 답변이 실제 코드베이스 상태와 안 맞아 재확인·재질문한" 사례가 한 번 더 나오면, `.claude/agents/coding.md`에 "사용자 결정을 반영하기 전에 그 결정이 현재 코드베이스에서 실현 가능한지 먼저 확인한다"는 규칙 추가를 검토한다.

## 2026-08-18
- tags: [phase2-3, places, SearchHistory, 길이검증, CharField, coding.md, test.md]
- category: 구현 실수
- 문제: `SearchHistory.keyword`가 `CharField(max_length=200)`인데, 검색 기록을 저장하는 코드가 사용자가 입력한 검색어 길이를 검증하지 않고 그대로 `create()`에 넘겼다. 200자를 넘는 검색어로 로그인 사용자가 검색하면 DB가 `DataError`를 던져 요청 전체가 500으로 죽는 [BLOCKING] 버그였다. review가 정적 분석만으로 찾아냈고, coding/test 단계는 정상 길이 입력으로만 확인해서 놓쳤다. 이 구체적 버그(CharField 길이 제한과 저장 전 입력 검증 불일치)는 이번이 처음 발견된 유형이지만, 이전에 있었던 "비로그인 API에서 무효 토큰 처리 누락"(2026-08-18 기록)과 같은 구조 — "저장/처리 전에 대비 안 한 입력값 하나 때문에 정상 흐름 전체가 죽는" 카테고리다. 이 프로젝트는 명소 설명, 검색어 등 자유 텍스트를 여러 CharField/TextField에 저장하므로 앞으로도 반복될 가능성이 높다고 판단해, 첫 발견 시점에 바로 규칙을 추가한다(2026-08-13 "경기 데이터 드림" 사례와 같은 기준 — 구조적으로 반복될 카테고리는 1회만 나와도 하네스에 반영).
- 개선: 사용자에게 "검색 자체를 200자로 제한" vs "검색은 전체로 하고 저장만 자르기" 중 선택을 물어(AskUserQuestion) 후자로 결정. `keyword[:200]`으로 잘라서 저장하도록 coding에서 수정하고 회귀 테스트를 추가함. 재발 방지를 위해 `.claude/agents/coding.md`의 "3. 구현하기" 절에 "사용자 입력을 CharField/TextField에 저장할 때는 모델의 `max_length`와 저장 전 입력 길이가 항상 일치하는 게 아니라는 것을 확인하고, 초과할 수 있는 입력이면 어떻게 처리할지(자르기/거부 등) 정하고 구현한다"는 규칙을, `.claude/agents/test.md`의 체크리스트 절에 "CharField/TextField에 저장되는 사용자 입력은 필드 `max_length`를 넘는 값으로도 테스트한다(DB 에러 없이 처리되는지 확인)"는 규칙을 추가했다.

## 2026-08-18
- tags: [phase2-3, 인프라, python인터프리터, 환경, 반복]
- category: 하네스 개선
- 문제: test/verify 에이전트가 이번에도 각자 "기본 `python` 명령에는 Django가 없고, Django/DRF가 설치된 인터프리터는 `C:/Users/SSAFY/AppData/Local/Programs/Python/Python311/python.exe`(Python 3.11)를 써야 한다"는 환경 함정을 스스로 찾아내는 데 시간을 썼다. Phase 2-2 때도 같은 함정을 각 에이전트가 개별적으로 발견했던 것과 동일 패턴이 이번이 2번째로 재현됐다. 지금까지 이 정보가 `CLAUDE.md` 등 하네스 문서 어디에도 적혀있지 않아서, 매 사이클 각 에이전트가 처음부터 다시 찾아내는 낭비가 반복되고 있었다.
- 개선: `CLAUDE.md`에 "개발 환경" 절을 새로 추가해, 이 프로젝트에서 Django/DRF가 설치된 파이썬 인터프리터 경로(`C:/Users/SSAFY/AppData/Local/Programs/Python/Python311/python.exe`)를 명시했다. `manage.py` 등을 실행하는 모든 에이전트(coding/test/verify)가 매번 새로 찾지 않고 이 경로를 바로 쓰도록 함.

## 2026-08-18
- tags: [phase2-3, 정상동작, 질문우선, review재검증, test스킵보고]
- category: 리뷰 누락 (정상 동작 사례)
- 문제 아님 — 이번 사이클에서 하네스가 의도대로 잘 작동한 사례. (1) review가 코드를 실제로 실행하지 않고 정적 분석만으로 `SearchHistory.keyword` 길이 검증 누락 [BLOCKING]을 정확히 찾아냈다 — review.md의 "소스코드를 근거로 로직 오류를 찾는다" 원칙이 그대로 작동했다. (2) 버그를 고칠 때 coding이 수정 방향(검색 자체를 제한할지, 저장만 자를지)을 스스로 정하지 않고 AskUserQuestion으로 사용자에게 먼저 물어 결정을 받았다 — karpathy-guidlines.md의 질문 우선 규칙이 다시 정상 작동했다. (3) 재검증 단계에서 test 에이전트가 브라우저 자동화 도구가 없어 스크린샷을 못 남기고 개발 DB에 직접 쓰는 명령이 샌드박스 정책으로 막혔는데, 임의로 다른 방법을 쓰지 않고 test.md의 "캡쳐할 수 없는 항목은 건너뛰고 이유를 보고서에 남긴다" 규칙대로 자동화 테스트로 대체하고 이유를 명시해서 보고했다. 세 번째 항목은 이번에 처음 나온 구체적 상황이라 아직 반복인지 알 수 없고, 기존 test.md 규칙만으로 이미 적절히 처리됐으므로 별도 규칙 추가는 필요 없다고 판단했다.
- 개선: 규칙이 의도대로 작동했으므로 하네스를 고치지 않음. review.md의 정적 분석 원칙, karpathy-guidlines.md의 질문 우선 규칙, test.md의 "캡쳐 불가 항목은 건너뛰고 이유를 남긴다" 규칙을 그대로 유지. 브라우저 자동화 도구 부재·DB 직접 쓰기 차단이 다음 사이클에도 반복되면, 그때는 test 에이전트의 도구 권한 설정 자체를 점검한다.

## 2026-08-18
- tags: [phase2-2, accounts, LocaleView, 인증, 비로그인API, coding.md, test.md]
- category: 구현 실수
- 문제: `docs/DETAIL_SPEC.md` 6-1 #9에 "로그인 여부와 상관없이 호출 가능"이라고 정해진 `PATCH /api/account/locale/`(`LocaleView`)이, 실제로는 무효·만료된 토큰을 보낸 요청에서 막혀버렸다. `FirebaseAuthentication.authenticate()`가 무효 토큰에 대해 `AuthenticationFailed` 예외를 던지면, DRF `dispatch()`가 `patch()`를 실행하기도 전에 401로 응답을 끝내버리기 때문이다("토큰 없음"은 익명으로 통과되지만 "토큰은 있는데 무효함"은 다르게 처리된다는 차이를 coding 단계에서 놓쳤다). verify(체크리스트 검증)와 coding 모두 이 케이스를 안 짚고 지나갔고, review가 정적 코드 분석으로만 이 문제를 [BLOCKING]으로 잡아냈다. 이 구체적 버그 자체는 이번이 첫 사례지만, "비로그인 허용 API"라는 카테고리는 DETAIL_SPEC에 이미 여러 건(#8 국적 저장, #9 locale) 나왔고 앞으로도 반복해서 만들 가능성이 높은 유형이라 판단해, 처음 발견 시점에 바로 규칙을 추가했다(과거 2026-08-13 "경기 데이터 드림 출처 검증" 사례와 같은 기준 — 구조적으로 반복될 카테고리는 1회만 나와도 하네스에 반영).
- 개선: 사용자에게 "무효 토큰을 익명으로 취급" vs "401 유지하고 문서만 정정" 중 선택을 물어(AskUserQuestion) "익명으로 취급"으로 결정. `LocaleView`에 `perform_authentication()`을 오버라이드해 이 뷰에서만 인증 예외를 삼키도록 coding에서 수정, 테스트 `test_invalid_token_is_treated_as_anonymous` 추가. 재발 방지를 위해 `.claude/agents/coding.md`의 "3. 구현하기" 절에 "로그인 여부와 상관없이 호출 가능한 API는 토큰 없음뿐 아니라 무효·만료 토큰 케이스도 처리해야 한다(필요하면 `perform_authentication()` 오버라이드)"는 규칙을 추가했고, `.claude/agents/test.md`의 체크리스트 항목에도 "무효·만료 토큰" 케이스를 명시적으로 추가했다.

## 2026-08-18
- tags: [phase2-2, review, agent툴, stall, 인프라]
- category: 하네스 개선 (아직 규칙 추가 안 함)
- 문제: Agent 툴로 review 에이전트를 처음 호출했을 때 "no progress for 600s (stream watchdog did not recover)"로 stall되어 실패했다. review 에이전트가 외부 명령(docker, 서버 기동 등 오래 걸리는 작업)을 시도하다 멈춘 것으로 보이며, "외부 명령 없이 정적 코드 읽기 위주로 진행하라"는 지시를 추가해 재시도하자 정상 완료됐다. 기존에 기록된 Workflow 툴의 "control characters" 오류(2026-08-13, 2026-08-17)와는 증상과 원인이 다른 별개의 인프라 문제이고, 이 구체적 증상(Agent 툴로 review 호출 시 외부 명령 때문에 stall)은 이번이 첫 사례다.
- 개선: 1회성 사례라 아직 `review.md`에 규칙을 추가하지 않는다. 같은 증상(review가 외부 명령을 시도하다 stall)이 한 번 더 나오면, `.claude/agents/review.md`에 "정적 코드 분석을 우선하고, 서버 기동·docker 등 오래 걸리는 외부 명령은 꼭 필요한 경우가 아니면 피한다"는 규칙 추가를 검토한다.

## 2026-08-18
- tags: [phase2-2, 정상동작, 질문우선, review재검증, DRF소스코드]
- category: 리뷰 누락 (정상 동작 사례)
- 문제 아님 — 이번 사이클에서 하네스가 의도대로 잘 작동한 사례. (1) review가 [BLOCKING]을 찾아낸 뒤, coding이 임의로 수정 방향을 정하지 않고 AskUserQuestion으로 "무효 토큰을 익명으로 취급"과 "401 유지·문서만 정정" 두 선택지를 사용자에게 먼저 물어보고 결정을 받았다 — karpathy-guidlines.md의 "불명확하면 멈추고 물어본다" 규칙이 그대로 작동한 사례. (2) 수정 후 재검증에서 review가 DRF 소스코드(`Request._authenticate()`, `APIView.initial()`)를 직접 읽고 수정이 의도대로 동작하는지, 다른 뷰(LoginView/MeView)에 영향은 없는지까지 논리적으로 확인한 뒤에 pass로 판정했다 — 추측이 아니라 근거를 직접 확인하는 review.md 원칙이 잘 지켜졌다.
- 개선: 규칙이 의도대로 작동했으므로 하네스를 고치지 않음. karpathy-guidlines.md의 질문 우선 규칙, review.md의 "추측만으로 문제를 만들어내지 않는다·근거를 확인한 것만 지적한다" 규칙을 그대로 유지.

## 2026-08-17
- tags: [phase2-2, accounts, 테스트버그, mock, URL리네임, test]
- category: 테스트 놓침
- 문제: Phase 2-2 test 단계에서 새 테스트를 작성하던 중, 기존 `accounts/tests.py`에 있던 실제 버그 두 가지를 우연히 발견했다. (1) `LOGIN_URL`/`ME_URL` 상수가 2026-08-13 URL 리네임 커밋(`/api/auth/` → `/api/account/`) 이후 갱신되지 않고 계속 옛 경로를 참조하고 있었다. (2) `LoginView` 관련 테스트들이 `accounts.views.verify_id_token`만 mocking했는데, 실제로는 전역 인증 클래스가 별도 위치(`accounts.authentication.verify_id_token`)에서 같은 함수를 다시 호출하고 있어, 헤더를 보내는 로그인 테스트 5건이 최초 구현 커밋부터 의도한 대로 검증되지 못하고 있었을 가능성이 있었다. 둘 다 "테스트 결과는 통과로 나왔지만 실제로는 오래전부터 의도한 대로 검증하지 못하고 있었는데 아무도 몰랐다"는 유형으로, 이전 기록(2026-08-17 경기 데이터 드림 건: "확인됐다고 기록된 조사 결과 자체가 틀렸음")과는 다른 각도지만 "겉으로 통과/확인된 것을 그대로 믿으면 안 된다"는 같은 교훈을 준다. 다만 이번 사이클에서 처음 발견된 구체적 유형(리네임 후 상수 미갱신, mock 대상 오지정)이라 아직 반복 패턴이라 단정하기는 이르다.
- 개선: 근본 원인이 테스트 코드 자체(URL 리네임 후 상수 미갱신, mock 대상 오지정)였고, test 에이전트가 테스트 파일만 고쳐서 즉시 해결했다(운영 코드는 건드리지 않음, review가 스코프를 재확인함). 아직 첫 사례라 test.md/review.md에 규칙을 새로 추가하지는 않는다. 다음에도 "리네임/리팩토링 이후 테스트에 남은 옛 경로·옛 이름"이나 "여러 위치에서 같은 함수를 호출하는데 mock을 한 곳에만 걸어 테스트가 의도대로 검증 못 하는" 유형이 한 번 더 나오면, test.md에 "리네임 후에는 테스트에서 옛 경로·옛 이름을 grep으로 재확인" 및 "mock을 걸 때는 그 함수의 실제 호출 지점을 전부 찾아본 뒤 건다"는 규칙 추가를 검토한다.

## 2026-08-17
- tags: [phase2-2, 정상동작, 질문우선, verify재검증, karpathy가이드라인]
- category: 리뷰 누락 (정상 동작 사례)
- 문제 아님 — 이번 사이클에서 하네스가 의도대로 잘 작동한 사례를 기록. (1) coding 에이전트가 착수 전에 기준 문서에 명시되지 않은 애매한 지점("비로그인 사용자의 국적을 어디에 저장하나")을 스스로 정하지 않고 사용자에게 먼저 물어 확정(프론트 localStorage)한 뒤 구현했다 — karpathy-guidlines.md 1번 규칙("불명확하면 멈추고 물어본다")이 그대로 작동한 사례. (2) verify 에이전트가 "국적 미선택 시 한국어로 보인다" 항목을 coding의 "새 로직 불필요" 판단만 믿지 않고 코드베이스 전체를 직접 검색해서 실제로 그런 분기 로직이 어디에도 없다는 걸 확인한 뒤 판정했다 — verify.md의 "이전 보고만 믿지 않고 직접 확인한다" 규칙이 2026-08-13에 이어 다시 정상 작동했다.
- 개선: 규칙이 의도대로 작동했으므로 하네스를 고치지 않음. karpathy-guidlines.md 1번 규칙, verify.md의 직접 재확인 규칙을 그대로 유지.

## 2026-08-17
- tags: [phase2-1, 경기데이터드림, 외부데이터출처, 문서정정]
- category: 문서 오류
- 문제: `docs/DETAIL_SPEC.md` 7장에 "경기 데이터 드림은 실제로 촬영지 데이터가 없어 제외됨 (2026-08-13 확인)"이라고 기록돼 있었는데, 이 결론 자체가 틀렸던 것으로 드러났다. 사용자가 실제로 경기 데이터 드림에 "촬영지원 현황" 데이터셋(시군명·작품명·촬영구분명·촬영연도·촬영장소명 제공)을 API 신청해 키를 발급받아왔고, WebSearch로 data.gg.go.kr에서 실재를 재확인했다. 2026-08-13 당시에도 WebSearch로 "확인"했다고 기록했지만 그 확인 자체가 부정확했다. 이전 사례("AI가 확인 없이 임의로 출처를 골랐던 문제")와는 반대 방향의 문제 — 이번엔 "확인됐다"고 기록된 조사 결과 자체가 틀렸던 사례다.
- 개선: `docs/DETAIL_SPEC.md` 7장을 취소선 처리 후 정정 반영함(경기 데이터 드림 포함, 후보 출처 전부에서 받아온 뒤 필요한 데이터만 거르는 방식으로 정리). 이번이 이 유형(기록된 조사 결과 자체가 오류)의 첫 사례라 아직 반복 패턴으로 보기는 이르므로 하네스 규칙은 추가하지 않음. 다음에도 "WebSearch로 확인함"이라고 기록된 내용이 사용자의 실제 확인(신청, 계약, 화면 확인 등)과 어긋나는 사례가 한 번 더 나오면, coding.md의 "확인 못 했다면 명시" 규칙을 "WebSearch로 확인한 내용도 최종 확정이 아니라 재검증 가능성을 열어둔다"는 방향으로 보강하는 것을 검토한다.

## 2026-08-17
- tags: [workflow툴, 인프라, 오케스트레이션, 반복]
- category: 하네스 개선
- 문제: coding→test→verify→review→compound를 자동 실행하는 Workflow 툴 호출이 2026-08-13에 이어 이번에도 동일한 에러("script contains control characters that would be hidden in the approval dialog")로 재현됐다 (2번째 반복 확인). 지난번엔 1회성 버그일 수 있다고 보고 규칙을 추가하지 않았는데, 이번에 같은 증상이 다시 나와 재발성이 있는 것으로 확인됐다. 이번에도 Agent 툴로 4단계를 수동 순서 호출하는 방식으로 대체해 문제없이 전부 완료했다.
- 개선: `CLAUDE.md`에 "알려진 도구 이슈" 절을 추가해, Workflow 툴 실패 시 Agent 툴로 coding→test→verify→review→compound를 수동 순서 호출하는 대체 절차를 명시함. 도구 자체의 버그라 근본 수정은 여전히 하네스 문서 밖의 일이지만, 재발할 때마다 대체 절차를 새로 판단하지 않도록 문서화함.

## 2026-08-17
- tags: [phase2-1, 정상동작, 외부API미연동, 회귀테스트, review]
- category: 리뷰 누락 (정상 동작 사례)
- 문제 아님 — 이번 사이클에서 하네스가 의도대로 잘 작동한 사례들을 기록. (1) coding 에이전트가 DETAIL_SPEC 7장 미해결 항목(응답 필드 미확정, 중복 판단 기준 없음) 때문에 실제 외부 API 연동 코드는 만들지 않고 로컬 JSON 샘플로만 구현했고, README에 "실제 API 필드명이 확정된 게 아니다"라고 명시함. (2) test 에이전트가 2026-08-13에 기록된 과거 버그 패턴(좌표 파싱 실패, 재수집 시 빈 값 덮어쓰기)을 회귀 테스트로 다시 검증함. (3) review 에이전트가 BLOCKING 없이 pass 판정하면서도 NIT 2건(source_id가 0일 때 falsy 판정 오류 가능성, 이름 없는 Place가 조용히 생성될 수 있는 점)을 "출처 확정 시 재점검 필요"로 남겨, 문제로 단정하지 않으면서도 놓치지 않았다.
- 개선: 규칙이 의도대로 작동했으므로 하네스를 고치지 않음. coding.md의 외부 데이터 출처 확인 규칙, test.md의 회귀 테스트 규칙(과거 버그 재확인), review.md의 BLOCKING/NIT 구분 규칙을 그대로 유지.

## 2026-08-13
- tags: [phase2-1, 명소데이터, 외부데이터출처, 가정검증]
- category: 구현 실수
- 문제: coding 에이전트가 명소·작품 데이터 임포트를 구현하면서, `docs/DETAIL_SPEC.md`에 "후보"로만 적혀있던 공공데이터 출처 4곳(KOBIS, 경기 데이터 드림, 한국문화정보원, 필름코리아) 중 "경기 데이터 드림"을 스스로 골라 필드명(장소명/소재지/위도/경도)까지 추측해서 구현했다. 사용자에게 확인을 요청했지만 "에이전트 임시 선택"이라는 표현으로만 알렸고 실재 여부는 검증하지 않아, 사용자도 처음엔 검증 없이 그대로 수락했다. 이후 WebSearch로 실제 조사한 결과 경기 데이터 드림에는 촬영지 데이터셋 자체가 없었다(있는 건 "영화 제작업체 현황"이라는 무관한 데이터). 틀린 가정이 한 차례 그대로 통과될 뻔한 사례.
- 개선: `.claude/agents/coding.md`의 "2. 기준 문서 확인하기" 절에 규칙 추가 — 기준 문서에 후보로만 적힌 외부 데이터 출처를 고를 때 실재 여부·필드가 확인되지 않았다면 스스로 확정하지 않고, 확인 못 했다는 사실을 명시한 뒤 확정은 사용자에게 맡기도록 함. `docs/DETAIL_SPEC.md`에도 "경기 데이터 드림은 실제로 촬영지 데이터가 없어 제외됨(2026-08-13 확인)"이 이미 반영되어 있다.

## 2026-08-13
- tags: [phase2-1, 명소데이터, import, 엣지케이스, review]
- category: 테스트 놓침
- 문제: 공공데이터 가져오기(import_places) 명령어에서 coding/test 단계는 "정상 데이터"로만 테스트해서 두 가지 실제 버그를 놓쳤다: (1) 좌표가 숫자가 아닌 문자열이면 DecimalField 저장 시 예외로 명령어 전체가 죽는 문제, (2) 재수집 데이터에 일부 필드가 빠져 있으면 그 값으로 기존 정상 값을 빈 값으로 덮어써 지워버리는 문제. review 에이전트가 코드를 읽으며 엣지 케이스를 추론해 두 건 모두 [BLOCKING]으로 잡아냈고, 1회 수정 루프로 해결됨. "정상 케이스뿐 아니라 예외 케이스도 테스트"라는 기존 규칙은 있었지만 값 누락/이상한 타입 같은 데이터 자체의 결함까지는 구체적으로 짚지 않아 실제로 놓쳤다.
- 개선: `.claude/agents/test.md`의 "1. 체크리스트 만들기" 절에 규칙 추가 — 외부 데이터를 가져와 저장하는 기능(공공데이터 임포트 등)은 정상 데이터만이 아니라 값이 없거나(None/빈 문자열), 형식이 이상하거나(숫자여야 할 값이 문자열), 일부 필드만 담긴 데이터로도 반드시 테스트하도록 명시함.

## 2026-08-13
- tags: [phase2-1, verify, 재검증]
- category: 리뷰 누락 (정상 동작 사례)
- 문제 아님 — verify 에이전트가 의도대로 잘 작동한 사례를 기록. 1차 verify에서 "공공데이터에서 명소 목록을 가져와 실제 데이터가 채워진다" 체크리스트 항목을, coding/test가 로컬 샘플 JSON으로만 검증하고 실제 API를 한 번도 호출하지 않았다는 이유로 FAIL 판정했다. coding/test의 "확인 필요" 보고를 그대로 믿지 않고 직접 재검증해서, 위 "외부 데이터 출처 검증 없이 가정" 문제를 사용자에게 다시 확인하게 만든 계기가 됐다.
- 개선: 규칙이 의도대로 작동했으므로 하네스를 고치지 않음. verify.md의 "이전에 통과했다는 보고만 믿고 O로 표시하지 않는다 — 직접 확인한다" 규칙을 그대로 유지.

## 2026-08-13
- tags: [phase2-1, workflow툴, 인프라, 오케스트레이션]
- category: 하네스 개선
- 문제: coding→test→verify→review→compound를 자동으로 순서대로 실행하는 Workflow 툴 호출이 권한 처리기 버그("script contains control characters that would be hidden in the approval dialog")로 두 번 다 실패했다. 이번 Phase 2-1 사이클 전체를 각 단계를 Agent 툴로 사람이 직접 순서대로 호출하는 방식으로 대체 진행했다. 작업 내용과는 무관한 도구 자체의 버그.
- 개선: 이 저장소의 문서(coding.md/test.md/review.md/verify.md/CLAUDE.md/karpathy-guidlines.md)로 고칠 수 있는 문제가 아니라서 규칙은 추가하지 않음. Workflow 툴 자체의 버그이므로 수정 권한 밖 — 재발 시 이번처럼 Agent 툴로 단계를 수동 순서 호출하는 방식으로 대체하면 된다는 점만 기록해둔다.
