# Phase 3 — 검색

## 목표

**로그인 없이 명소와 작품을 검색할 수 있다.**

## 이 단계에서 만드는 것

| 기능 | PRD 번호 |
|---|---|
| 통합검색 (장소+작품) | S-03 |
| 드라마/영화 구분 조회 | S-03 |
| 자동완성 · 최근 검색어 | S-03 |
| 추천(인기) 검색어 | (BE에 이미 구현돼 있어 함께 진행, 2026-08-30 사용자 확인) |

## 하는 일

### 1. 검색 실행

- `GET /search/spots`, `GET /search/works` (둘 다 계획, DETAIL_SPEC §9)를 호출해 결과를 **장소 섹션 / 작품 섹션**으로 나눠 보여준다.
- 검색어가 비어 있으면 요청을 보내지 않는다.
- 결과가 없으면 "검색결과가 존재하지 않습니다"를 보여준다.

### 2. 드라마/영화 필터

- 작품만, 드라마만, 영화만 골라볼 수 있는 선택지를 제공한다.

### 3. 자동완성 · 최근 검색어

- 입력하는 동안 이름 후보를 보여준다(자동완성).
- 로그인한 사용자의 검색어는 서버에 남는다(BE가 처리, FE는 검색 API만 정상 호출하면 됨).
- 비로그인 사용자의 최근 검색어는 기기(로컬 저장)에만 보관하고 서버로 보내지 않는다.

### 4. 추천(인기) 검색어

- `GET /api/search/popular/` 호출해 검색 홈에 해시태그(`#검색어`) 형태로 보여준다.

## 진행 상황 (2026-08-30)

**구현 완료.**

| 파일 | 역할 |
|---|---|
| `src/api/search.ts` | `searchPlaces`, `getAutocomplete`, `getPopularKeywords` |
| `src/hooks/useRecentSearches.ts` | 최근 검색어 localStorage 저장/조회/삭제 (로그인 여부 상관없이 기기 저장 — DETAIL_SPEC S-03 참고) |
| `src/pages/SearchPage.tsx` | 검색 홈(추천/최근 검색어, 자동완성) + 검색 결과(필터, 작품/명소 섹션) |
| `src/App.tsx` | `/search` 라우트 추가 (로그인 불필요) |

Figma 실제 목업(node-id `102:1241` 검색 홈, `102:1265` 검색 결과)을 사용자가 공유해줘서 그 기준으로 만들었다. 목업에 없던 "최근 검색어"·"드라마/영화/작품 필터" UI는 기존 화면 스타일(칩, `LanguageSheet` 선택 표시)에 맞춰 직접 구성했다. 자세한 결정 내용은 `docs/DETAIL_SPEC.md` S-03 참고.

**Commands**
- `npm run test`: 통과 (검색 API 함수 성공/실패/결과없음 처리 테스트 추가, 총 30개)
- `npm run lint`: passed
- `npm run build`: passed

**브라우저 확인은 아직 안 함** — 이 세션에서 Claude in Chrome 확장을 쓰지 않기로 해서, 코드/테스트로만 확인했다. `npm run dev`(로컬 5174 포트)와 로컬 BE 서버는 켜둔 상태이니, 실제 화면에서 아래 체크리스트를 직접 눌러보고 확인해야 한다.

## 완료 기준 체크리스트

- [x] (코드/테스트로 확인) 검색하면 장소/작품 결과가 섹션으로 나뉘어 보인다
- [x] (코드로 확인, 브라우저 확인 필요) 드라마만/영화만 필터가 동작한다 (전체/드라마/영화 3개 칩 — "작품" 옵션은 2026-08-30 사용자 확인 후 제거)
- [x] (코드/테스트로 확인) 검색어가 비어 있으면 검색 요청을 보내지 않는다
- [x] (코드/테스트로 확인) 결과가 없으면 "검색결과가 존재하지 않습니다"가 보인다
- [ ] (브라우저 확인 필요) 자동완성이 동작한다
- [x] (코드로 확인, 브라우저 확인 필요) 최근 검색어는 기기에만 저장된다 (로그인 여부 상관없이, DETAIL_SPEC S-03 결정 참고)
- [x] 관련 유닛 테스트(검색 API 함수 성공/실패, 결과 없음 처리) 통과 (Vitest)
- [x] `npm run lint`, `npm run build` 통과

## 넘어가기 전 확인

- 검색어를 FE가 번역해서 보내지 않는다. BE가 저장된 번역 이름으로 이미 매칭해준다(BE DETAIL_SPEC 3-2, "검색어를 먼저 번역하지 않는 이유" 참고).
- 명소 상세 화면(Phase4)이 아직 없다. 검색 결과를 클릭했을 때 이동은 임시로 처리해도 된다.
